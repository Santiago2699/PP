let u = 5 * 2 + 10 / 2 - 2;;

let v = log ((1. +. 1./. 10000.)**10000.);;

let w = char_of_int (40 + 43);;

let x = String.length "longitud" > String.length "length";;

let y = if 'j' > 's' then "La 'j' despues de 's'" 
        else "La 's' va despues de la 'j'";;